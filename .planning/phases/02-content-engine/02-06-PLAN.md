---
phase: 02-content-engine
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - wp-content/plugins/artricenter-content/includes/class-shortcodes.php
  - wp-content/plugins/artricenter-content/templates/shortcodes/doctores-grid.php
  - wp-content/plugins/artricenter-content/templates/shortcodes/especialidades-list.php
  - wp-content/plugins/artricenter-content/templates/shortcodes/mission-cards.php
  - wp-content/plugins/artricenter-content/templates/page-homepage.php
  - wp-content/plugins/artricenter-content/templates/page-especialidades.php
autonomous: true
requirements: [PAGES-01, PAGES-02]
gap_closure: true

must_haves:
  truths:
    - "Homepage displays doctors grid via [artricenter_doctores_grid] shortcode"
    - "Homepage displays mission/vision/values cards via [artricenter_mission_cards] shortcode"
    - "Especialidades page displays specialties list via [especialidades_list] shortcode"
    - "Shortcode templates exist in wp-content/plugins/artricenter-content/templates/shortcodes/"
    - "Page templates use do_shortcode() to execute shortcodes (not placeholder comments)"
  artifacts:
    - path: "wp-content/plugins/artricenter-content/includes/class-shortcodes.php"
      provides: "Shortcode registration and handlers"
      exports: ["register_shortcodes", "render_doctores_grid", "render_mission_cards", "render_especialidades_list"]
      min_lines: 80
    - path: "wp-content/plugins/artricenter-content/templates/shortcodes/doctores-grid.php"
      provides: "Doctors grid display template"
      contains: "WP_Query doctor posts"
      min_lines: 40
    - path: "wp-content/plugins/artricenter-content/templates/shortcodes/especialidades-list.php"
      provides: "Specialties list display template"
      contains: "WP_Query especialidad posts"
      min_lines: 30
    - path: "wp-content/plugins/artricenter-content/templates/shortcodes/mission-cards.php"
      provides: "Mission/vision/values cards template"
      contains: "hardcoded HTML cards"
      min_lines: 30
  key_links:
    - from: "class-shortcodes.php"
      to: "WordPress shortcode API"
      via: "add_shortcode() calls"
      pattern: "add_shortcode\\('(artricenter_doctores_grid|artricenter_mission_cards|especialidades_list)'"
    - from: "shortcode templates"
      to: "CPT data"
      via: "WP_Query in template files"
      pattern: "WP_Query.*post_type.*='(doctor|especialidad)'"
    - from: "page-homepage.php"
      to: "shortcodes"
      via: "do_shortcode() calls"
      pattern: "do_shortcode\\('\\[artricenter_(doctores_grid|mission_cards)\\]'\\)"
    - from: "page-especialidades.php"
      to: "shortcodes"
      via: "do_shortcode() call"
      pattern: "do_shortcode\\('\\[especialidades_list\\]'\\)"
---

<objective>
Recreate missing shortcode implementation for dynamic CPT content display on homepage and especialidades page

Purpose: Close verification gap where class-shortcodes.php and all shortcode template files were staged but then deleted from filesystem, blocking PAGES-01 and PAGES-02 requirements. Page templates currently display placeholder text instead of dynamic CPT content.

Output: Functional shortcodes ([artricenter_doctores_grid], [artricenter_mission_cards], [especialidades_list]) with template files, integrated into page templates via do_shortcode() calls
</objective>

<execution_context>
@/home/zurybr/.claude/get-shit-done/workflows/execute-plan.md
@/home/zurybr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-engine/02-CONTEXT.md
@.planning/phases/02-content-engine/02-content-engine-VERIFICATION.md

# Interface Context

From 02-05-SUMMARY.md (previous shortcode implementation):
- Shortcodes class uses ob_start() + ob_get_clean() pattern for clean template capture
- WP_Query limits: doctors grid (3 posts), specialties list (-1 for all)
- Template includes use plugin_dir_path(__FILE__) with ../templates/ relative path
- Shortcode handlers use shortcode_atts() for default values and sanitization
- Mission cards hardcoded for v1 (Misión, Visión, Valores with color coding)

From 02-04-SUMMARY.md (page templates):
- Homepage template has placeholder comments at lines 25-30 (doctors) and 35-40 (mission cards)
- Especialidades template has placeholder comment at lines 20-25 (specialties list)
- Placeholders need replacement with do_shortcode() calls

From 02-03-SUMMARY.md (CPT structure):
- Doctores CPT: post_type='doctor', rewrite_slug='doctor-artricenter'
- Especialidades CPT: post_type='especialidad', rewrite_slug='especialidad-artricenter'
- Sucursales CPT: post_type='sucursal', rewrite_slug='sucursal-artricenter'

From existing plugin structure (02-01-SUMMARY.md):
- artricenter-content.php uses direct class loading (no PSR-4 autoloader)
- Classes loaded via require_once in includes/ directory
- Activation hook registration pattern established
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Shortcodes class with 3 shortcode handlers</name>
  <files>wp-content/plugins/artricenter-content/includes/class-shortcodes.php</files>
  <action>
Create class-shortcodes.php with:

1. Class declaration: class Shortcodes
2. Constructor: register_shortcodes() method calling add_shortcode() for 3 shortcodes:
   - [artricenter_doctores_grid] → render_doctores_grid()
   - [artricenter_mission_cards] → render_mission_cards()
   - [especialidades_list] → render_especialidades_list()
3. render_doctores_grid($atts) method:
   - Use shortcode_atts() with default 'limit' => 3
   - Start output buffering with ob_start()
   - Include template: plugin_dir_path(__FILE__) . '../templates/shortcodes/doctores-grid.php'
   - Return ob_get_clean()
4. render_mission_cards($atts) method:
   - Start output buffering
   - Include template: plugin_dir_path(__FILE__) . '../templates/shortcodes/mission-cards.php'
   - Return ob_get_clean()
5. render_especialidades_list($atts) method:
   - Use shortcode_atts() with default 'limit' => -1 (all)
   - Start output buffering
   - Include template: plugin_dir_path(__FILE__) . '../templates/shortcodes/especialidades-list.php'
   - Return ob_get_clean()

Security: All shortcode handlers must return strings (no direct echo). Use esc_html__() for text translation.
  </action>
  <verify>
    <automated>grep -n "class Shortcodes" wp-content/plugins/artricenter-content/includes/class-shortcodes.php && grep -n "add_shortcode" wp-content/plugins/artricenter-content/includes/class-shortcodes.php | wc -l | grep -q "3"</automated>
  </verify>
  <done>Shortcodes class exists with 3 add_shortcode() calls and 3 render methods using output buffering</done>
</task>

<task type="auto">
  <name>Task 2: Create doctors grid shortcode template</name>
  <files>wp-content/plugins/artricenter-content/templates/shortcodes/doctores-grid.php</files>
  <action>
Create doctores-grid.php template with:

1. WP_Query to fetch doctor posts:
   - post_type => 'doctor'
   - posts_per_page => $limit (from shortcode attribute, default 3)
   - orderby => 'menu_order title'
   - order => 'ASC'
2. Check if have_posts()
3. If no doctors: display message "No hay doctores disponibles."
4. If doctors exist:
   - Open container div: &lt;div class="artricenter-doctors-grid"&gt;
   - Loop through doctors with while($query->have_posts())
   - For each doctor:
     - Get meta: _doctor_specialty, _doctor_location
     - Get featured image: get_the_post_thumbnail($post->ID, 'medium')
     - Get permalink: get_permalink()
     - Output card HTML with:
       - Doctor photo (featured image or placeholder)
       - Doctor name (the_title())
       - Specialty (esc_html($specialty))
       - Location (esc_html($location))
       - Profile link (permalink)
   - Close container div
5. wp_reset_postdata()

Use .artricenter-doctors-grid and .artricenter-doctor-card CSS classes for styling.
  </action>
  <verify>
    <automated>grep -n "WP_Query" wp-content/plugins/artricenter-content/templates/shortcodes/doctores-grid.php && grep -n "post_type.*doctor" wp-content/plugins/artricenter-content/templates/shortcodes/doctores-grid.php</automated>
  </verify>
  <done>Template queries doctor CPT and displays grid with photo, name, specialty, location, and profile link</done>
</task>

<task type="auto">
  <name>Task 3: Create mission cards shortcode template</name>
  <files>wp-content/plugins/artricenter-content/templates/shortcodes/mission-cards.php</files>
  <action>
Create mission-cards.php template with:

1. Container div: &lt;div class="artricenter-mission-cards"&gt;
2. Hardcoded HTML for 3 cards (Misión, Visión, Valores):
   - **Misión card**:
     - Title: "Misión"
     - Color: blue (.artricenter-mission-card--blue)
     - Icon: target or heart icon
     - Content: "Brindar atención médica especializada en reumatología con calidez humana y tecnología de vanguardia."
   - **Visión card**:
     - Title: "Visión"
     - Color: green (.artricenter-mission-card--green)
     - Icon: eye or star icon
     - Content: "Ser el referente nacional en el tratamiento de enfermedades reumáticas, mejorando la calidad de vida de nuestros pacientes."
   - **Valores card**:
     - Title: "Valores"
     - Color: orange (.artricenter-mission-card--orange)
     - Icon: shield or checkmark icon
     - Content: "Compromiso, excelencia, empatía e innovación en cada consulta y tratamiento."
3. Close container div

Each card structure:
- &lt;div class="artricenter-mission-card artricenter-mission-card--{color}"&gt;
  - Icon circle with background
  - &lt;h3&gt; title
  - &lt;p&gt; content
- &lt;/div&gt;

Content hardcoded per 02-05-SUMMARY decision (v1 simplicity, editable via options page in future).
  </action>
  <verify>
    <automated>grep -n "artricenter-mission-card" wp-content/plugins/artricenter-content/templates/shortcodes/mission-cards.php | wc -l | grep -q "3"</automated>
  </verify>
  <done>Template displays 3 hardcoded mission/vision/values cards with color coding (blue, green, orange)</done>
</task>

<task type="auto">
  <name>Task 4: Create specialties list shortcode template</name>
  <files>wp-content/plugins/artricenter-content/templates/shortcodes/especialidades-list.php</files>
  <action>
Create especialidades-list.php template with:

1. WP_Query to fetch especialidad posts:
   - post_type => 'especialidad'
   - posts_per_page => $limit (from shortcode attribute, default -1 for all)
   - orderby => 'title'
   - order => 'ASC'
2. Check if have_posts()
3. If no specialties: display message "No hay especialidades disponibles."
4. If specialties exist:
   - Open container div: &lt;div class="artricenter-especialidades-list"&gt;
   - Loop through specialties with while($query->have_posts())
   - For each specialty:
     - Get featured image: get_the_post_thumbnail($post->ID, 'thumbnail')
     - Get permalink: get_permalink()
     - Get excerpt: get_the_excerpt() or manual content limit
     - Output list item HTML with:
       - Specialty icon/image (featured image or default icon)
       - Specialty name (the_title())
       - Description (excerpt or content)
       - "Más información" link (permalink)
   - Close container div
5. wp_reset_postdata()

Use .artricenter-especialidades-list and .artricenter-especialidad-item CSS classes.
List from CONTEXT.md: artrosis, artritis reumatoide, fibromialgia, espondilitis anquilosante, reumatismos de partes blandas.
  </action>
  <verify>
    <automated>grep -n "WP_Query" wp-content/plugins/artricenter-content/templates/shortcodes/especialidades-list.php && grep -n "post_type.*especialidad" wp-content/plugins/artricenter-content/templates/shortcodes/especialidades-list.php</automated>
  </verify>
  <done>Template queries especialidad CPT and displays list with icon, name, description, and permalink</done>
</task>

<task type="auto">
  <name>Task 5: Integrate shortcodes into homepage template</name>
  <files>wp-content/plugins/artricenter-content/templates/page-homepage.php</files>
  <action>
Update page-homepage.php to replace placeholder comments with do_shortcode() calls:

1. Find "Nuestros Médicos" section (around line 25-30 per 02-04-SUMMARY)
2. Replace placeholder comment with:
   ```php
   <?php echo do_shortcode('[artricenter_doctores_grid]'); ?>
   ```
3. Find "Misión, Visión y Valores" section (around line 35-40)
4. Replace placeholder comment with:
   ```php
   <?php echo do_shortcode('[artricenter_mission_cards]'); ?>
   ```

Ensure shortcodes are wrapped in appropriate section markup with existing CSS classes (.artricenter-homepage__doctors, .artricenter-homepage__mission).
  </action>
  <verify>
    <automated>grep -n "do_shortcode.*artricenter_doctores_grid" wp-content/plugins/artricenter-content/templates/page-homepage.php && grep -n "do_shortcode.*artricenter_mission_cards" wp-content/plugins/artricenter-content/templates/page-homepage.php</automated>
  </verify>
  <done>Homepage template executes doctores_grid and mission_cards shortcodes via do_shortcode() calls</done>
</task>

<task type="auto">
  <name>Task 6: Integrate shortcode into especialidades template</name>
  <files>wp-content/plugins/artricenter-content/templates/page-especialidades.php</files>
  <action>
Update page-especialidades.php to replace placeholder comment with do_shortcode() call:

1. Find specialties listing section (around line 20-25 per 02-04-SUMMARY)
2. Replace placeholder comment with:
   ```php
   <?php echo do_shortcode('[especialidades_list]'); ?>
   ```

Ensure shortcode is wrapped in appropriate section markup with existing CSS class (.artricenter-especialidades__list).
  </action>
  <verify>
    <automated>grep -n "do_shortcode.*especialidades_list" wp-content/plugins/artricenter-content/templates/page-especialidades.php</automated>
  </verify>
  <done>Especialidades template executes especialidades_list shortcode via do_shortcode() call</done>
</task>

<task type="auto">
  <name>Task 7: Register Shortcodes class in main plugin file</name>
  <files>wp-content/plugins/artricenter-content/artricenter-content.php</files>
  <action>
Update artricenter-content.php to load and instantiate Shortcodes class:

1. Add require_once for class-shortcodes.php after existing includes:
   ```php
   require_once ARTRICENTER_CONTENT_PLUGIN_DIR . 'includes/class-shortcodes.php';
   ```
2. Instantiate Shortcodes class after other class instantiations:
   ```php
   $artricenter_shortcodes = new \Artricenter\Content\Shortcodes();
   $artricenter_shortcodes->register_shortcodes();
   ```

Follow existing pattern from CPT classes (02-01-SUMMARY).
  </action>
  <verify>
    <automated>grep -n "class-shortcodes.php" wp-content/plugins/artricenter-content/artricenter-content.php && grep -n "new.*Shortcodes" wp-content/plugins/artricenter-content/artricenter-content.php</automated>
  </verify>
  <done>Shortcodes class loaded and instantiated in main plugin file with register_shortcodes() called</done>
</task>

</tasks>

<verification>
Overall phase verification steps:

1. **File existence**: All 4 shortcode files exist in plugin directory
2. **Shortcode registration**: All 3 shortcodes registered via add_shortcode()
3. **Template integration**: Page templates use do_shortcode() (not placeholder comments)
4. **CPT query patterns**: Templates use WP_Query with correct post_type values
5. **Output buffering**: All render methods use ob_start()/ob_get_clean() pattern
6. **Security**: No direct echo in shortcode handlers, all return strings

Manual verification:
- Create test doctor and especialidad entries in WordPress admin
- Visit homepage and verify doctors grid and mission cards display
- Visit especialidades page and verify specialties list displays
- Verify no PHP errors in browser console or debug.log
</verification>

<success_criteria>
- [ ] class-shortcodes.php exists with 3 shortcode handlers
- [ ] All 3 shortcode template files exist in templates/shortcodes/
- [ ] Homepage template uses do_shortcode() for doctors grid and mission cards
- [ ] Especialidades template uses do_shortcode() for specialties list
- [ ] Shortcodes class registered in main plugin file
- [ ] All shortcode handlers use output buffering pattern
- [ ] Templates query correct CPT post types (doctor, especialidad)
- [ ] No placeholder comments remain in page templates
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-engine/02-06-SUMMARY.md`
</output>
