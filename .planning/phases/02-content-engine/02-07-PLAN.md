---
phase: 02-content-engine
plan: 07
type: execute
wave: 3
depends_on: [02-03]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CPT-03, CPT-04]
gap_closure: true

must_haves:
  truths:
    - "CPT-03 requirement marked as [x] Complete in REQUIREMENTS.md"
    - "CPT-04 requirement marked as [x] Complete in REQUIREMENTS.md"
    - "Traceability table shows CPT-03 and CPT-04 as Phase 2 Complete"
    - "REQUIREMENTS.md reflects actual implementation status (Sucursales CPT with unique rewrite slug)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements tracking document"
      contains: "CPT-03 and CPT-04 marked as [x] Complete"
      min_lines: 163
  key_links:
    - from: "REQUIREMENTS.md CPT-03 checkbox"
      to: "actual implementation"
      via: "verification evidence"
      pattern: "\\[x\\].*CPT-03"
    - from: "REQUIREMENTS.md CPT-04 checkbox"
      to: "actual implementation"
      via: "verification evidence"
      pattern: "\\[x\\].*CPT-04"
---

<objective>
Update REQUIREMENTS.md to mark CPT-03 and CPT-04 as complete, closing documentation gap

Purpose: Close verification gap where REQUIREMENTS.md shows CPT-03 and CPT-04 as "[ ] Pending" despite Sucursales CPT being fully implemented with unique rewrite slug (verified in 02-03-SUMMARY and VERIFICATION.md). Documentation must reflect actual implementation status.

Output: REQUIREMENTS.md with CPT-03 and CPT-04 marked as [x] Complete, traceability table updated to show Phase 2 Complete status
</objective>

<execution_context>
@/home/zurybr/.claude/get-shit-done/workflows/execute-plan.md
@/home/zurybr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-content-engine/02-content-engine-VERIFICATION.md
@.planning/phases/02-content-engine/02-03-SUMMARY.md

# Verification Gap Details

From VERIFICATION.md gap #2:
- **Truth affected:** "All three CPTs use unique rewrite slugs to avoid permalink conflicts"
- **Root cause:** REQUIREMENTS.md shows CPT-03 and CPT-04 as "[ ] Pending" despite implementation
- **Evidence:** 02-03-SUMMARY confirms Sucursales CPT implemented with:
  - Post type: 'sucursal'
  - Rewrite slug: 'sucursal-artricenter' (unique per CPT-04)
  - 174 lines with location meta boxes, color scheme validation
  - Verification via wp-cli post-type list showing all 3 CPTs with unique slugs

From 02-03-SUMMARY:
- CPT-03 satisfied: "Sucursales CPT registered with meta boxes for name, address, phone, maps link, color scheme"
- CPT-04 satisfied: "All three CPTs use unique rewrite slugs ending in '-artricenter', preventing permalink conflicts"
- Automated verification confirmed: `sucursal {"slug":"sucursal-artricenter"}`

Current REQUIREMENTS.md state (lines 36-37):
```markdown
- [ ] **CPT-03**: WordPress admin can manage Sucursales Custom Post Type with fields: name, address, phone, Google Maps link, color scheme (blue/green/orange)
- [ ] **CPT-04**: Custom Post Types use unique rewrite slugs (e.g., `doctor-artricenter`) to avoid permalink conflicts with pages
```

Required changes:
1. Line 36: Change `[ ]` to `[x]` for CPT-03
2. Line 37: Change `[ ]` to `[x]` for CPT-04
3. Update traceability table (lines 129-130) to show Phase 2 status as "Complete" for CPT-03 and CPT-04
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CPT-03 checkbox to Complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update REQUIREMENTS.md line 36 to mark CPT-03 as complete:

Find line:
```markdown
- [ ] **CPT-03**: WordPress admin can manage Sucursales Custom Post Type with fields: name, address, phone, Google Maps link, color scheme (blue/green/orange)
```

Replace with:
```markdown
- [x] **CPT-03**: WordPress admin can manage Sucursales Custom Post Type with fields: name, address, phone, Google Maps link, color scheme (blue/green/orange)
```

Evidence: 02-03-SUMMARY confirms Sucursales CPT implemented with all required fields (address, phone, maps link, color scheme) and 174-line class-sucursales.php file.
  </action>
  <verify>
    <automated>grep -n "CPT-03" .planning/REQUIREMENTS.md | head -1 | grep -q "\\[x\\]"</automated>
  </verify>
  <done>CPT-03 requirement marked as [x] Complete in REQUIREMENTS.md</done>
</task>

<task type="auto">
  <name>Task 2: Update CPT-04 checkbox to Complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update REQUIREMENTS.md line 37 to mark CPT-04 as complete:

Find line:
```markdown
- [ ] **CPT-04**: Custom Post Types use unique rewrite slugs (e.g., `doctor-artricenter`) to avoid permalink conflicts with pages
```

Replace with:
```markdown
- [x] **CPT-04**: Custom Post Types use unique rewrite slugs (e.g., `doctor-artricenter`) to avoid permalink conflicts with pages
```

Evidence: 02-03-SUMMARY confirms all 3 CPTs use unique rewrite slugs:
- doctor-artricenter (Doctores CPT)
- especialidad-artricenter (Especialidades CPT)
- sucursal-artricenter (Sucursales CPT)

Automated verification via wp-cli: `sucursal {"slug":"sucursal-artricenter"}`

No permalink conflicts possible due to -artricenter suffix on all slugs.
  </action>
  <verify>
    <automated>grep -n "CPT-04" .planning/REQUIREMENTS.md | head -1 | grep -q "\\[x\\]"</automated>
  </verify>
  <done>CPT-04 requirement marked as [x] Complete in REQUIREMENTS.md</done>
</task>

<task type="auto">
  <name>Task 3: Update traceability table for CPT-03 and CPT-04</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Update REQUIREMENTS.md traceability table (around lines 129-130) to reflect CPT-03 and CPT-04 completion:

Find lines in traceability table:
```markdown
| CPT-03 | Phase 2 | Pending |
| CPT-04 | Phase 2 | Pending |
```

Replace with:
```markdown
| CPT-03 | Phase 2 | Complete |
| CPT-04 | Phase 2 | Complete |
```

This ensures the traceability section at the end of REQUIREMENTS.md matches the actual implementation status confirmed in 02-03-SUMMARY and VERIFICATION.md.
  </action>
  <verify>
    <automated>grep -A1 "| CPT-03" .planning/REQUIREMENTS.md | tail -1 | grep -q "Complete"</automated>
  </verify>
  <done>Traceability table shows CPT-03 and CPT-04 as Phase 2 Complete</done>
</task>

</tasks>

<verification>
Overall phase verification steps:

1. **CPT-03 checkbox**: Changed from [ ] to [x]
2. **CPT-04 checkbox**: Changed from [ ] to [x]
3. **Traceability table**: Both requirements show "Complete" status
4. **Consistency**: REQUIREMENTS.md now matches 02-03-SUMMARY and VERIFICATION.md evidence
5. **No other changes**: Only CPT-03 and CPT-04 modified, all other requirements unchanged

Verification that changes align with actual implementation:
- Sucursales CPT exists (class-sucursales.php, 174 lines)
- Unique rewrite slug 'sucursal-artricenter' confirmed via wp-cli
- All required meta boxes implemented (address, phone, maps, color)
- CPT-04 unique slug requirement satisfied (all 3 CPTs use -artricenter suffix)
</verification>

<success_criteria>
- [ ] CPT-03 marked as [x] Complete in requirements list (line 36)
- [ ] CPT-04 marked as [x] Complete in requirements list (line 37)
- [ ] Traceability table shows CPT-03 as Phase 2 Complete
- [ ] Traceability table shows CPT-04 as Phase 2 Complete
- [ ] REQUIREMENTS.md consistent with 02-03-SUMMARY implementation evidence
- [ ] No other requirements modified
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-engine/02-07-SUMMARY.md`
</output>
