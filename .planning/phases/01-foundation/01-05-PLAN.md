---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - wp-content/plugins/artricenter-structure/includes/class-hooks.php
  - wp-content/plugins/artricenter-structure/artricenter-structure.php
autonomous: true
requirements:
  - STRUCT-05
user_setup: []
must_haves:
  truths:
    - Plugin registers custom WordPress hooks for content injection
    - artricenter_before_content hook fires before main content area
    - artricenter_after_content hook fires after main content area
    - Hooks documented with PHPDoc comments
    - Other plugins can hook into these actions
  artifacts:
    - path: wp-content/plugins/artricenter-structure/includes/class-hooks.php
      provides: Hook registration class
      contains: register_hooks(), do_action() calls
    - path: wp-content/plugins/artricenter-structure/artricenter-structure.php
      provides: Main plugin file
      contains: Hook class initialization
  key_links:
    - from: class-hooks.php
      to: WordPress action system
      via: add_action(), do_action()
      pattern: add_action.*wp.*do_action.*artricenter
    - from: other plugins
      to: custom hooks
      via: add_action('artricenter_before_content', ...)
      pattern: add_action\\('artricenter_
---

<objective>
Register WordPress hooks for other plugins to inject content before and after main content area.

Purpose: Provide extension points for future plugins (content engine, interactive features) to inject components without modifying theme templates.

Output: Custom hooks registered and firing, documentation for hook usage, example hook implementation in plugin.
</objective>

<execution_context>
@/home/zurybr/.claude/get-shit-done/workflows/execute-plan.md
@/home/zurybr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.agents/skills/wordpress-pro/SKILL.md
@.agents/skills/wp-plugin-development/SKILL.md

# Locked decisions from CONTEXT.md
- Hook registration: Register custom hooks for other plugins to inject content
- do_action('artricenter_before_content') — before main content area
- do_action('artricenter_after_content') — after main content area
- Priority: Run hooks on wp hook with priority 10
- Documentation: Add PHPDoc comments for hook usage

# WordPress hooks pattern from RESEARCH.md
- Custom actions enable plugin extensibility
- do_action() fires hooks that other plugins can hook into
- add_action() registers callback functions
- Priority parameter controls execution order

# Hook naming convention
- Prefix with plugin name (artricenter_)
- Descriptive action name (before_content, after_content)
- Use snake_case (WordPress standard)

# Integration with theme templates
- Hooks fire automatically on wp action
- Theme doesn't need to call hooks directly
- Plugins can add_action() to inject content
- Content appears before/after main content area

# Example usage for future plugins
```php
// In another plugin:
add_action('artricenter_before_content', 'my_custom_banner');
function my_custom_banner() {
    echo '<div class="custom-banner">Welcome!</div>';
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hooks registration class</name>
  <files>wp-content/plugins/artricenter-structure/includes/class-hooks.php</files>
  <action>
Create Hooks class with:

1. **Class structure**:
   - Namespace: Artricenter\Structure
   - Method: register_hooks() registers hook callbacks
   - Method: fire_before_content() fires artricenter_before_content hook
   - Method: fire_after_content() fires artricenter_after_content hook

2. **Hook registration**:
   ```php
   /**
    * Register custom hooks for content injection.
    *
    * Registers hooks that fire before and after the main content area,
    * allowing other plugins to inject content without modifying theme templates.
    *
    * @since 1.0.0
    */
   public function register_hooks(): void {
       // Fire hooks on the 'wp' action (after WordPress is fully loaded)
       add_action( 'wp', array( $this, 'fire_before_content' ), 10 );
       add_action( 'wp', array( $this, 'fire_after_content' ), 10 );
   }
   ```

3. **Fire before content hook**:
   ```php
   /**
    * Fire the 'artricenter_before_content' hook.
    *
    * This hook fires before the main content area, allowing plugins
    * to inject banners, announcements, or other content.
    *
    * Example usage:
    * ```php
    * add_action( 'artricenter_before_content', 'my_custom_banner' );
    * function my_custom_banner() {
    *     echo '<div class="custom-banner">Welcome!</div>';
    * }
    * ```
    *
    * @since 1.0.0
    */
   public function fire_before_content(): void {
       do_action( 'artricenter_before_content' );
   }
   ```

4. **Fire after content hook**:
   ```php
   /**
    * Fire the 'artricenter_after_content' hook.
    *
    * This hook fires after the main content area, allowing plugins
    * to inject CTAs, related content, or other elements.
    *
    * Example usage:
    * ```php
    * add_action( 'artricenter_after_content', 'my_custom_cta' );
    * function my_custom_cta() {
    *     echo '<div class="custom-cta">Contact us today!</div>';
    * }
    * ```
    *
    * @since 1.0.0
    */
   public function fire_after_content(): void {
       do_action( 'artricenter_after_content' );
   }
   ```

5. **PHPDoc comments**:
   - Include @since tag (version when hook was added)
   - Include example usage in code comments
   - Describe when hook fires and what it's for

6. **Hook naming**:
   - artricenter_before_content (fires before main content)
   - artricenter_after_content (fires after main content)

Follow WordPress Coding Standards (WPCS). Use proper indentation and formatting.
  </action>
  <verify>
    <automated>grep -q "class Hooks" wp-content/plugins/artricenter-structure/includes/class-hooks.php && grep -q "do_action( 'artricenter_before_content' )" wp-content/plugins/artricenter-structure/includes/class-hooks.php && grep -q "do_action( 'artricenter_after_content' )" wp-content/plugins/artricenter-structure/includes/class-hooks.php && grep -q "add_action( 'wp'" wp-content/plugins/artricenter-structure/includes/class-hooks.php && echo "Hooks class valid"</automated>
  </verify>
  <done>
  - class-hooks.php exists with Hooks class
  - register_hooks() method registers hook callbacks
  - fire_before_content() fires artricenter_before_content hook
  - fire_after_content() fires artricenter_after_content hook
  - Hooks registered on wp action with priority 10
  - PHPDoc comments with @since tags
  - Example usage in code comments
  - do_action() calls present for both hooks
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize Hooks class in main plugin file</name>
  <files>wp-content/plugins/artricenter-structure/artricenter-structure.php</files>
  <action>
Update artricenter-structure.php:

1. **Load Hooks class**:
   - Autoloader already loads classes from includes/
   - No additional require_once needed

2. **Initialize Hooks class in Plugin main class**:
   ```php
   /**
    * Initialize the plugin.
    *
    * @since 1.0.0
    */
   public function run(): void {
       // Register navigation menus
       $navigation = new \Artricenter\Structure\Navigation();
       $navigation->register_menus();

       // Register custom hooks
       $hooks = new \Artricenter\Structure\Hooks();
       $hooks->register_hooks();

       // Enqueue assets
       $this->enqueue_assets();
   }
   ```

3. **Enqueue assets method** (if not already present):
   ```php
   /**
    * Enqueue plugin assets.
    *
    * @since 1.0.0
    */
   private function enqueue_assets(): void {
       add_action( 'wp_enqueue_scripts', array( $this, 'enqueue_styles' ) );
       add_action( 'wp_enqueue_scripts', array( $this, 'enqueue_scripts' ) );
   }

   /**
    * Enqueue plugin stylesheets.
    *
    * @since 1.0.0
    */
   public function enqueue_styles(): void {
       // Enqueue variables first
       wp_enqueue_style(
           'artricenter-structure-variables',
           plugins_url( 'assets/css/variables.css', __FILE__ ),
           array(),
           '1.0.0',
           'all'
       );

       // Enqueue main styles (depends on variables)
       wp_enqueue_style(
           'artricenter-structure-style',
           plugins_url( 'assets/css/main.css', __FILE__ ),
           array( 'artricenter-structure-variables' ),
           '1.0.0',
           'all'
       );
   }

   /**
    * Enqueue plugin scripts.
    *
    * @since 1.0.0
     */
   public function enqueue_scripts(): void {
       wp_enqueue_script(
           'artricenter-mobile-menu',
           plugins_url( 'assets/js/mobile-menu.js', __FILE__ ),
           array(),
           '1.0.0',
           true
       );
   }
   ```

4. **Update Plugin class**:
   - Add Hooks initialization to run() method
   - Move asset enqueuing to Plugin class (if previously in template-tags.php)
   - Ensure proper order: menus → hooks → assets

5. **Clean up template-tags.php** (if needed):
   - Remove asset enqueuing if moved to Plugin class
   - Keep template tag functions (artricenter_the_header(), artricenter_the_footer())
   - Keep menu registration callback

Maintain WordPress Coding Standards throughout.
  </action>
  <verify>
    <automated>grep -q "new \\\\Artricenter\\\\\\\\Structure\\\\\\\\Hooks()" wp-content/plugins/artricenter-structure/artricenter-structure.php && grep -q "->register_hooks()" wp-content/plugins/artricenter-structure/artricenter-structure.php && echo "Hooks class initialized"</automated>
  </verify>
  <done>
  - Hooks class instantiated in Plugin->run()
  - register_hooks() method called
  - Asset enqueuing organized in Plugin class
  - Proper initialization order maintained
  - No duplicate hook registrations
  - Plugin class structure clean and organized
  </done>
</task>

<task type="auto">
  <name>Task 3: Create hook documentation and example</name>
  <files>wp-content/plugins/artricenter-structure/README.md</files>
  <action>
Create or update README.md with:

1. **Plugin description**:
   ```markdown
   # Artricenter Structure Plugin

   Provides site-wide layout components (header, footer, navigation) and global CSS for Artricenter WordPress site.

   ## Features

   - Responsive header with logo and navigation (mobile overlay + desktop horizontal menu)
   - Footer with 3 sucursales cards (La Raza, Atizapán, Viaducto) and info section
   - Namespaced CSS (.artricenter-) to prevent theme conflicts
   - Smooth scroll navigation for in-page anchors
   - Custom hooks for plugin extensibility

   ## Installation

   1. Upload `artricenter-structure` folder to `/wp-content/plugins/`
   2. Activate plugin in WordPress admin (Plugins → Add New)
   3. Add template tags to theme:
      - Header: `<?php artricenter_the_header(); ?>` in theme's `header.php`
      - Footer: `<?php artricenter_the_footer(); ?>` in theme's `footer.php`

   ## Custom Hooks

   ### artricenter_before_content

   Fires before the main content area. Use for banners, announcements, or content that should appear at the top of every page.

   **Example:**
   ```php
   add_action( 'artricenter_before_content', 'my_custom_banner' );
   function my_custom_banner() {
       echo '<div class="custom-banner">Welcome to Artricenter!</div>';
   }
   ```

   ### artricenter_after_content

   Fires after the main content area. Use for CTAs, related content, or elements that should appear at the bottom of every page.

   **Example:**
   ```php
   add_action( 'artricenter_after_content', 'my_custom_cta' );
   function my_custom_cta() {
       echo '<div class="custom-cta">Contact us today!</div>';
   }
   ```

   ## Template Tags

   ### artricenter_the_header()
   Displays the Artricenter header with logo and responsive navigation.

   ### artricenter_the_footer()
   Displays the Artricenter footer with sucursales cards and info section.

   ## CSS Namespace

   All CSS classes use the `.artricenter-` prefix to prevent conflicts with WordPress themes.

   Example:
   - `.artricenter-header` (not `.header`)
   - `.artricenter-footer` (not `.footer`)
   - `.artricenter-nav` (not `.nav`)

   ## Requirements

   - WordPress 6.9.4+
   - PHP 8.2+
   - Theme with `wp_head()` and `wp_footer()` hooks

   ## License

   GPL v2 or later
   ```

2. **Hook documentation**:
   - Clear description of when each hook fires
   - Code examples for common use cases
   - Parameter descriptions (if any)
   - @since tags for version tracking

3. **Installation instructions**:
   - How to install plugin
   - How to add template tags to theme
   - Requirements (WordPress version, PHP version)

4. **Usage examples**:
   - Template tag usage
   - Hook usage
   - CSS namespace explanation

5. **License information**:
   - GPL v2 or later
   - Compatible with WordPress plugin repository

Use clear markdown formatting with code blocks for examples.
  </action>
  <verify>
    <automated>grep -q "artricenter_before_content" wp-content/plugins/artricenter-structure/README.md && grep -q "artricenter_after_content" wp-content/plugins/artricenter-structure/README.md && grep -q "add_action" wp-content/plugins/artricenter-structure/README.md && echo "Hook documentation present"</automated>
  </verify>
  <done>
  - README.md exists with plugin description
  - Custom hooks documented (artricenter_before_content, artricenter_after_content)
  - Code examples provided for each hook
  - Template tags documented (artricenter_the_header, artricenter_the_footer)
  - Installation instructions present
  - CSS namespace explanation included
  - Requirements specified (WordPress 6.9.4+, PHP 8.2+)
  - License information included
  </done>
</task>

</tasks>

<verification>
1. Reactivate plugin (wp-admin/plugins.php)
2. Verify plugin activates without errors
3. Create test plugin to verify hooks work:
   - Create wp-content/plugins/test-hooks/test-hooks.php
   - Add: `<?php add_action('artricenter_before_content', function() { echo '<div style="background: yellow; padding: 20px;">BEFORE CONTENT HOOK FIRED</div>'; }); ?>`
   - Add: `<?php add_action('artricenter_after_content', function() { echo '<div style="background: green; padding: 20px; color: white;">AFTER CONTENT HOOK FIRED</div>'; }); ?>`
   - Activate test plugin
4. Visit homepage, verify:
   - Yellow banner appears before content
   - Green banner appears after content
   - Both banners display on all pages
5. Verify hook documentation:
   - Read README.md
   - Check hook descriptions are clear
   - Verify code examples work
6. Verify hook firing order:
   - Check that artricenter_before_content fires before main content
   - Check that artricenter_after_content fires after main content
7. Verify no conflicts:
   - Deactivate test plugin
   - Verify site still works normally
   - Verify no PHP errors in debug.log
8. Verify hook priority:
   - Hooks should fire on wp action with priority 10
   - Content should appear in correct position
9. Test hook with content:
   - Create a plugin that adds a banner via artricenter_before_content
   - Verify banner displays on all pages
   - Verify banner appears before header/footer content
</verification>

<success_criteria>
1. Hooks class exists and is initialized in main plugin file
2. artricenter_before_content hook fires before main content area
3. artricenter_after_content hook fires after main content area
4. Hooks registered on wp action with priority 10
5. Test plugin can hook into custom hooks successfully
6. Hook content displays in correct position (before/after content)
7. README.md documents both hooks with clear examples
8. PHPDoc comments present with @since tags
9. No PHP errors when hooks fire
10. Plugin activates and deactivates without issues
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` with:
- Hook implementation summary (artricenter_before_content, artricenter_after_content)
- Hook registration details (wp action, priority 10)
- Documentation and examples provided
- Test results (hook firing, content injection)
- Integration points for future plugins
- All 5 plans complete summary
- Phase 1 foundation fully implemented
- Next steps for Phase 2: Content Engine
</output>
